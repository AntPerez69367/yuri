//! Shared `#[repr(C)]` game types — re-exported from `crate::common::types`.
//!
//! Import from here or directly from `crate::common::types`; both paths work.
pub use crate::common::types::{GfxViewer, GlobalReg, Item, Point};
